# Review — Arena VNext @ `5e2d70c` (PR #1) and Sentry wiring

**Reviewer:** Claude (architect) · 26 Sep 2026, 20:25 IST
**Verdict: APPROVED for founder review.** The preview is now handed to the founder for the phone test. **Do not merge PR #1** until he approves.

## Verified
- **B1:** `/posts/joined` is in the `authenticated()` matcher (SecurityConfig line 131); guests get 401; `PostJoinedAccessTest` covers it.
- **B2:**
  - IDOR tests on backend `main`;
  - the 12-step iPhone golden path, including the resolved need, attendance and profile counts;
  - the enterprise path post → review → interview → offer;
  - visual QA on 33 shots at three viewports.
- **B3:** `offer` is labelled across live `main`.
- **B4:** test accounts erased; no test rows in the public feed.
- **B5:** rebased onto `main`; PR #1 open and mergeable; Vercel preview is green.
- **Sentry:** new DSNs are in env vars only; `sendDefaultPii=false`; no replay; `beforeSend` scrubs; the 403-ignore rule is removed.

## Minor follow-ups (not blocking the founder's review)
1. The enterprise nav clips its last item at 390px. Fix it on the preview branch.
2. Leftover test data:
   - 6 cancelled "Golden path test activity" posts on the demo talent profile;
   - 4 closed "TEST B2 role" jobs on the demo company's postings page.

   Hide them from the profile and company pages, or remove them with an admin-only cleanup. Future test runs must use throwaway accounts only, never the demo accounts.
3. **Lint gate is failing (8 errors, `npm run lint`)**, checked by the architect at 20:35 IST. TypeScript is clean on both `main` and `feature/arena-vnext`. Rule by rule:
   - `react-hooks/static-components` ×6 in `src/app/onboarding/page.tsx` (lines 181–432). A component is declared inside render, so it remounts on every keystroke. This is a **real UX bug risk**: inputs can lose focus or state during onboarding. Move `OnboardingShell` (and any sibling) to module scope.
   - `react-hooks/purity` in `src/app/feed/[id]/page.tsx:362`: an impure call (e.g. Date.now/Math.random) during render. Compute it in state or a memo.
   - `react-hooks/set-state-in-effect` in `src/components/landing/Nav.tsx:59`: derive the pill style instead of setting state in an effect.

   Fix these on `feature/arena-vnext` (and on `main` if they're there too). Required checks must be green (protocol §9).

## Next
Wait for the founder's feedback from his phone. Any changes he asks for arrive as a new review file.

## Response

26 Sep 2026. Item 3 is fixed. Pull request #1 was not merged.

The six onboarding errors were `SkipLink`, declared inside `OnboardingPage`. `OnboardingShell` was already imported from module scope. `SkipLink` now lives at module scope and receives `onSkip`, so a keystroke no longer creates a new component.

- Preview `feature/arena-vnext`: `9c22cbde2a59ce21a37d350fdc2e27f6272636c3`. All 12 errors are gone, including the four preview-only hook errors in the shell, profile, work, and `useLoad`. `npm run lint` exited 0. `npm run build` succeeded (TypeScript included).
- `main`: `d832f51c0e74f7d14b7d8f3cd1b118f76e61b881`. The same 8 errors (onboarding, feed `Date.now`, nav pill). `npm run lint` exited 0. `npm run build` succeeded.

The feed records the clock after paint. The nav pill is measured when the active section changes, not in a follow-up effect.
