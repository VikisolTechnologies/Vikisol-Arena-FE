{
  "framework": "nextjs",
  "buildCommand": "echo \"NEXT_PUBLIC_BUILD_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo local)\" >> .env.production.local && echo \"NEXT_PUBLIC_BUILD_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)\" >> .env.production.local && next build"
}
